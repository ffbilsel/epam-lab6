#!/usr/bin/env sh
# Constitution v1.1.0 / Principle V.8 — pre-commit gate.
# Runs typecheck + lint + unit tests. Integration / E2E / mutation run in CI
# only to keep commits fast.
set -e

cd "$(dirname "$0")/.."

npm run typecheck
npm run lint
npm run test:unit
