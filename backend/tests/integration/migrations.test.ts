// Placeholder for tasks.md T026 (testcontainer Postgres + migrations).
import { describe, it } from '@jest/globals';

describe.skip('migrations', () => {
  it('runs up & down cleanly against Postgres 16', () => undefined);
});
