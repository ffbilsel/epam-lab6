// Placeholder for tasks.md T031, T043, T053 — quickstart journeys end-to-end.
import { describe, it } from '@jest/globals';

describe.skip('e2e journeys', () => {
  it('Journey 1: register + verify + sign-in', () => undefined);
  it('Journey 2 + 4: login + lockout', () => undefined);
  it('Journey 3: password reset', () => undefined);
});
